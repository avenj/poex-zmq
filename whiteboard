# POEx::ZMQ

# POEx::ZMQ::{Publisher,Subscriber,Router,Dealer,Requestor,Replier}
   consumes POEx::ZMQ::Role::Socket

# POEx::ZMQ::Role::Socket
   has context
     * The user is responsible for providing a context.
       We can export a factory util for creating one as a singleton.
   consumes POEx::ZMQ::Role::EmitEvents
   provides backend-y methods & POE integration

# POEx::ZMQ::Role::EmitEvents
  consumes MooX::Role::POE::Emitter


# POEx::ZMQ::FFI
  # POEx::ZMQ::FFI::Context
    can ->destroy_ctx
    can ->create_socket
    can ->get_ctx_opt
    can ->set_ctx_opt
    calls ->destroy_ctx in DEMOLISH
     ffi can ->zmq_ctx_new
         can ->zmq_ctx_set
         can ->zmq_ctx_get
         can ->zmq_ctx_destroy

  # POEx::ZMQ::FFI::Socket
    
